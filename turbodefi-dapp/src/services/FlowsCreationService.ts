import { ContractFunction } from "@multiversx/sdk-core/out";
import { sendTransaction } from "./contractInteractions";

/*
export const CreateFlow = async (address:string, flowName: string, flowDescription: string) => {
    await sendTransaction({
        contractAddress: address,
        functionName: new ContractFunction('claimRewards'),
        sender
    });
};
*/