export enum RouteNamesEnum {
  home = '/',
  unlock = '/unlock',
  disclaimer = '/disclaimer',
  scanner = '/scanner',
  project = '/project/:projectName?',
  ashSwap = '/ashSwap',
  staking = '/staking',
  flows = '/flows',
  flow = '/flow/:flowId?',
  dshboard = '/dashboard'
}
